# Add these variables in the correct Vercel project and/or existing .env.local.
# Do not replace an existing .env.local file or commit actual credentials.
FOURTHWALL_STOREFRONT_TOKEN=
FOURTHWALL_READONLY_ENABLED=false
# Fill only after /v1/shop returns the correct shop and its real id.
FOURTHWALL_EXPECTED_SHOP_ID=
